import styled from "styled-components";

export const Button = styled.button`
  margin: 2rem 0;
  padding: 4px 8px;
  border-radius: 4px;
  border: 1px solid #909090;
  background: none;

  font-size: 14px;
  font-weight: 400;
  color: #909090;

  &:hover {
    cursor: pointer;
  }
`;
